name: Build and Test React Application
#Controls when the action will run 
on:
#Triggers the workflow on push or pull request events but only the main branch 
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
